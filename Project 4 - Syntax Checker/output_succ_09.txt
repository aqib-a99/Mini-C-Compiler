Success: no syntax error is found.

Following is the indentation-updated source code:

function main(a::int, b::int, a::int)::int
begin
    var x::int;
    x := x + 1;
    while 1 = 2
    begin
        var y::int;
        x := x() + (2 - 3) * (y + 5 / 6 / z[(1 + 2) / 2 + w[1]]);
    end
end

